# Usado automaticamente pelo `vite build` (e portanto pelo `npm run dist`).
# O instalador gerado aponta para a API de produção no Render.
# Para gerar um instalador apontando para outro servidor, mude aqui e rode
# `npm run dist` de novo.
VITE_API_URL=https://solucao-backend.onrender.com
